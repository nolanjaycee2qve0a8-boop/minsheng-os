window.MinshengFileRegistry = (() => {const supported=['pdf','csv','json','txt','xlsx','xls'];const validate=file=>!supported.includes((file.fileType||'').toLowerCase())?'当前仅支持登记 PDF、CSV、JSON、TXT、XLSX、XLS。':null;const duplicate=(files,file)=>files.find(x=>!x.deleted&&((file.checksum&&x.checksum===file.checksum)||(x.fileName===file.fileName&&x.size===file.size&&x.sourceDocumentId===file.sourceDocumentId)));return {supported,validate,duplicate};})();
