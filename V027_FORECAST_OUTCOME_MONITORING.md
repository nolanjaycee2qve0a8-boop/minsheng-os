# v0.27 情景结果监测与事前预测回测门禁

v0.27 建立预测登记簿、官方结果记录、语义匹配、时间门禁、评价运行与汇总，但不新增预测模型或优化参数。

## 严格分类

`SYSTEM_PRE_REGISTERED_FORECAST` 与 `SOURCE_PUBLISHED_FORECAST` 才可能进入误差评价。v0.16–v0.26 既有运行默认仍是 `SCENARIO_ONLY_NOT_FORECAST`、`STRESS_SCENARIO_NOT_FORECAST` 或 `RESEARCH_PROXY_SCENARIO`。事后重构为 `RETROSPECTIVE_RECONSTRUCTION`，只可解释，不能回填为事前预测。

## 门禁

每个登记保存目标、期间、时间、范围/点值、口径、来源、版本和冻结信息。冻结登记不可改写；任何修改创建新的 revision。结果必须是 `REAL` 且标记为首次、修订或最新实际值，预测和估计不能成为 actual。

预测与结果必须匹配概念、地域、期间、频率、单位、名义/实际、累计/期间/时点、季调、人口范围和转换定义。时间未知、结果发布后回填、口径不一致或非实际值全部阻断正式预测误差。

## 评价

- 点预测：`forecast − actual` 与绝对误差；增长率使用百分点，不默认计算百分比误差。
- 方向预测：必须在登记时提供 baseline 和方向。
- v0.26 envelope：只报告实际值在范围内、低于或高于范围及最近边界距离。

确定性区间不是统计置信区间；不附概率，不称覆盖率或概率校准。scenario 与结果可以比较，但永不进入 MAE、RMSE 或模型排名。

v0.24 case 仅能逐一监测其 child result；`NO_CROSS_SECTOR_TOTAL` 继续生效，单个宏观结果不能验证整个跨部门 case。

## Vintage 和聚合

首次发布与最新修订分别保存。结果修订只使依赖旧结果的最新 vintage 评价失效，首次发布评价保留。MAE、RMSE 和平均有符号误差仅在至少两条同概念、地域、频率、单位与口径一致的合格评价上生成，且始终带 `NO_MODEL_RANKING`。

## LIVE 锚点

唯一 LIVE 锚点复用 v0.8.2 已保存的 World Bank 2025 中国实际 GDP 增长预测（4.5%，2025-06-13 发布）与 NBS 2025 官方首次发布实际值（5.0%，2026-02-28 发布）。误差为 -0.5 个百分点；它不是新造数据，也不足以排名机构。

所有评价运行均不可变、可确定性导出；不会写入 REAL、正式损失或因果边。总体因果验证继续为 `PARTIAL`。
